import LoginForm from "../components/forms/LoginForm";

const Login = () => {
  return (
    <div className="w-full h-full bg-gradient-to-br from-gray-300 to-slate-400 flex justify-center items-center">
      <LoginForm />
    </div>
  );
};

export default Login;
